`range(10)`: [0, 10), start is not specified, thus from 0 by default

`range(0, 10)`: [0, 10), both start and end are specified, step is by default 1

`range(0, 10, 1)`: [0, 10), step is specified as well
